# Some reference values for use in both ui.R and server.R
all.tests <- c("two-sided", "one-sided (upper)", "one-sided (lower)")
all.hyp <- c("Null (everyone is healthy)", "Alternative (everyone is sick)")
normal.temp <- 37
normal.sd <- 0.5

